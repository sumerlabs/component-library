export type SliderProps = {
    children: React.ReactNode;
    hideArrows?: boolean;
    hideDots?: boolean;
    dragMode?: boolean;
    iconBackArrow?: JSX.Element;
    iconNextArrow?: JSX.Element;
}